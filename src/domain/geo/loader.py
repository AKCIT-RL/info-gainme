"""Geo graph loader (skeleton).

Loads a small hierarchical graph from `data/world_flat.csv` for the benchmark.
"""

from __future__ import annotations


def load_geo_graph(sample_size: int = 50):  # type: ignore[no-untyped-def]
    raise NotImplementedError


