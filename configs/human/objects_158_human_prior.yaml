# Human Baseline — Objects (158 objects, With Hint)
# Mode: WH — user sees full candidate list once before game starts, then it disappears
# Backend: same as PO (PARTIALLY_OBSERVABLE), the hint is frontend-only

models:
  seeker:
    model: "human"

  oracle:
    model: "Qwen3-8B"
    timeout: 300.0

  pruner:
    model: "Qwen3-8B"
    timeout: 300.0

game:
  observability_mode: "PARTIALLY_OBSERVABLE"
  max_turns: 30

dataset:
  type: "objects"
  csv_path: "data/objects/objects_full.csv"
  num_targets: null
  runs_per_target: 1

experiment:
  name: "objects_158_human_prior"
  max_workers: 1
  tags:
    seeker_type: "human"

output:
  base_dir: "outputs"
  save_conversations: true
  save_graph_plots: false

debug:
  enabled: true
